import 'package:flutter/material.dart';

class FamilyView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Familia"),
      ),
      body: Text(
          "Estou na family view"

      ),
    );
  }
}
