import 'package:flutter/material.dart';

class EditMemoryView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Lembrete"),
      ),
      body: Text(
          "Estou na addremidner view"

      ),
    );
  }
}
