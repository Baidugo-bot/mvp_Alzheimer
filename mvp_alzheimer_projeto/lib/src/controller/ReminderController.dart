import 'package:flutter/material.dart';

class ReminderView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Lembrete"),
      ),
      body: Text(
       "Estou na lembrete view"

    ),
    );
  }
}
